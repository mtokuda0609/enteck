<h1>ユーザ情報編集</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for @company, url: {action: 'confirm'} do |f| %>
      <%= render partial: 'form', locals: {f: f} %>
    <% end %>
  </div>
</div>